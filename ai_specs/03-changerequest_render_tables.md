# refine rendering of tables in epub
- currently a table is just printing the table contents as plain text
- refine the rendering, so that rows and columns are separated with lines and with a box surrounding the table
- also the header of the table should be designed as usual
- use the file 7epubs/assets/example-with-table.epub as example