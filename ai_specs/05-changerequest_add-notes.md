# add notes to an epub
- refine _PageToolbar
- remove colors except yellow, green and red
- add an icon to take a note and the corresponding GestureDetector onTakeNote
- implement the note taking 
  - open a dialog for entering the note's text
  - Save the note in the same way as the highlighted text is saved
  - highlight the word that was selected when clicking the "take note" icon with blue color
- refine the AppBar of 7epubs
  - add an three-dots icon to display a menu with these items
    - display all notes in a list on a separate screen
    - export all notes as a markdown file via a file selektor
    - export all notes as a json file via a file selektor
    